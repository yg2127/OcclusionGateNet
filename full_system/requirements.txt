# Runtime deps for the integrated Full DMS system (full_dms_system/).
# The classifier/ and landmark/ packages at the repo root are reused as-is,
# so their deps (einops, yacs) are listed here too.

# Core
torch>=2.0
torchvision>=0.15
numpy>=1.24
opencv-python>=4.7
PyYAML>=6.0
tqdm>=4.65

# Detectors / landmarks
ultralytics>=8.0          # YOLO-Pose (body) + YOLO-face (face bbox)
mediapipe>=0.10           # FaceMesh (478 landmarks)

# ORFormer + HGNet landmark restoration (reuses ../landmark)
einops>=0.6               # ORFormer ViT (landmark/src/models/simple_vit.py)
yacs>=0.1.8               # landmark config (landmark/configs/default.py)

# Figures / analysis
matplotlib>=3.7
pandas>=2.0
scikit-learn>=1.3

# Optional: only for scripts/run_full_system_warning_overlay_edge_tts_hardcoded_v2.py
# edge-tts>=6.1           # neural TTS for spoken warnings (also needs the `ffmpeg` binary)
